programa

numero a, b, c.
booleano t, f, g.

a := 1.75.
b := 3.
t := verdadeiro.
f := falso.

c := a+b.
escreva(c).
c := a*b.
escreva(c).
c := b-a.
escreva(c).
c := b/a.
escreva(c).

g := f&&t.
escreva(g).
g := f||t.
escreva(g).
g := (a==b).
escreva(g).
g := (a!=b).
escreva(g).
g := (a<=b).
escreva(g).
g := (a<b).
escreva(g).
g := (b<=a).
escreva(g).
g := (b<a).
escreva(g).

g := t&&(b<a).
escreva(g).
a :=b.
g := (b<a).
escreva(g).

fimprog.